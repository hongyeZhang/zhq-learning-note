JavaScript 笔记
=================          Chapter4          ===================

引用类型的值 是保存在内存中的对象，是按照引用访问的
所有函数的参数都是按值传递的

没有块级作用域
if(true) {
    var color = "blue";
}
alert(color);

垃圾收集策略：
（1）标记清除
（2）引用计数
引用类型的值是对象，保存在堆内存中；基本类型保存在栈内存中。
栈的优势是，存取速度比堆要快，仅次于寄存器，栈数据可以共享。
但缺点是，存在栈中的数据大小与生存期必须是确定的，缺乏灵活性。
栈中主要存放一些基本类型的变量（,int, short, long, byte, float, double, boolean, char）和对象句柄。
栈有一个很重要的特殊性，就是存在栈中的数据可以共享

花括号:
（1）表达式上下文
（2）语句上下文

访问对象的属性：
（1）方括号表示法

    var person = {name: "zhq"
                    , age : "11"};
    alert(person["name"]);
（2）点表示法

Array.isArray()确定是否是数组
数组的栈操作：push() pop()
数组的队列操作：push()  shift()
