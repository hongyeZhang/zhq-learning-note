创建文件夹  mkdir <fileName>

数文件有几行：  wc -l a.txt



=====================    创建文件   =================================
(1) touch a.txt
(2) 可以使用cat创建一个新的文件
　　命令：cat>>filename
ctrl + D  保存并退出
cat file1 file2 >> file3 命令也可将多个文件合并到一个新的文件当中。首先创建一个newfile1并输入内容然后合并。
